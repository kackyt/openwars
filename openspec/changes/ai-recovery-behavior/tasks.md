## 1. AI行動評価ルーチンの修正（engine.rs）

- [x] 1.1 `decide_ai_action` 内で「戦闘不能（Combat Ineffective）」状態（HPが40未満、または主武装の弾薬が0）を判定する変数を追加する。
- [x] 1.2 戦闘不能状態のユニットに対して、通常の待機（何もない平地での `Wait`）や低期待値の `Attack` に大きなスコアペナルティを与える。

## 2. 拠点撤退（Retreat to Property）のスコアリング実装

- [x] 2.1 タイル評価ループ内で、対象の到達可能マスが自軍の拠点（回復・補給可能）であるか判定するロジックを追加する。
- [x] 2.2 戦闘不能なユニットが自軍の拠点マスに到達して `Wait` する場合、非常に高いスコア（例: +8000）を与えて撤退行動を優先させる。
- [x] 2.3 複数の回復拠点が到達範囲にある場合、現在地からの距離などをもとに最も適切な撤退先が選ばれるよう微調整する。

## 3. 部隊合流（Merge）のスコアリング実装

- [x] 3.1 `decide_ai_action` のループ内で、`actions.can_merge` が true となるタイルの評価ブロックを追加する。
- [x] 3.2 合流先のユニットが同一タイプかつ自軍であることを確認し、合流後のHP合計値などを加味して `Merge` アクションに高いスコア（例: +7000）を与える。

## 4. テストと動作確認

- [x] 4.1 低HPのユニットが拠点で待機する行動を選ぶことを確認する単体テストを追加する。
- [x] 4.2 弾薬切れのユニットが拠点で待機する行動を選ぶことを確認する単体テストを追加する。
- [x] 4.3 ダメージを受けたユニットが `Merge` コマンドを選択することを確認する単体テストを追加する。
