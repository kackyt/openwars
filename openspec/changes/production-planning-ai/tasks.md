## 1. 基礎データ構造とアルゴリズムの導入

- [x] 1.1 動的計画法（ナップサック問題）によるユニット選択ロジックの実装
- [x] 1.2 コストとスコアに基づくユニット候補の重み付けの実装
- [x] 1.3 `GamePhase` 枚挙型の定義（Expansion, Contested, Assault, Defense）
- [x] 1.4 `ProductionStrategy` 構造体の定義（現在のフェーズ、目標とするユニット構成比率、優先ターゲット）
- [x] 1.5 `ProductionPlan` リソースの定義（貯金状況、次ターン生産予約）

## 2. 戦略分析（Strategy Analysis）の実装

- [x] 2.1 マップの状態（拠点占領数、敵との距離）から `GamePhase` を判定するロジックの実装
- [x] 2.2 現在の自軍ユニット構成と理想比率を比較し、不足している役割を抽出するロジックの実装
- [x] 2.3 敵軍の構成をスキャンし、支配的なユニット種別に対するカウンターユニットを特定するロジックの実装
- [x] 2.4 `analyze_strategy` 関数の実装と、各種フェーズへの遷移テストの作成

## 3. スコアリングエンジンの刷新

- [x] 3.1 ユニットの基礎スコア計算を分離
- [x] 3.2 `StrategyBonus`（フェーズに応じた補正）の実装
- [x] 3.3 `CounterBonus`（アンチユニット補正）の実装
- [x] 3.4 ユニットの `BaseValue` 算出ロジック（機動力・射程の加味）の実装
- [x] 3.5 拠点から前線/未占領拠点への到達ターン数（ETA）に基づく `EtaPenalty` の実装

## 4. 資金計画（Budgeting）ロジックの実装

- [x] 4.1 緊急時の生産優先（貯金無視）ロジックの実装
- [x] 4.2 予算無制限での「理想ターゲット」の選定と予約金のロックロジックの実装
- [x] 4.3 ロックされた予約金を差し引いた「仮想予算」でナップサック問題を解くロジックへの改修
## 5. 思考エンジン（Tactical AI）の強化

- [x] 5.1 `decide_ai_action` による戦術的アクション決定ロジックの実装
- [x] 5.2 カミカゼアタック（無謀な攻撃）を回避する `is_suicidal_attack` の実装
- [x] 5.3 間接攻撃ユニットの射程維持ロジックの導入
- [x] 5.4 占領・合流・輸送（Load/Supply）アクションのAI対応
- [x] 5.5 `AiActionCooldown` による1ステップずつのAI実行制御の実装

## 6. 統合と検証

- [ ] 6.1 `decide_production` 内で `analyze_strategy` を呼び出し、各ボーナスを統合
- [ ] 6.2 統合テストの作成（「中戦車のために貯金する」「敵が戦車を出したら対戦車ユニットを作る」等のシナリオ）
- [x] 6.3 `cargo clippy` および `cargo fmt` の実行
- [x] 6.4 AIターンの全自動シミュレーション機能の実装 (`execute_ai_turn`)

## 7. 輸送（Load/Transport）協調ロジックの実装

- [ ] 7.1 生産時のETA計算における、空の輸送車同席によるペナルティ軽減ロジック
- [ ] 7.2 歩兵余り状態（輸送需要）を検知し、輸送ユニットに戦略ボーナスを与えるロジック
- [ ] 7.3 行動AIにおける輸送車と歩兵のランデブー（タクシー迎え・乗り込み）ロジックの拡張
