## 1. 戦略解析の拡張

- [x] 1.1 `ProductionStrategy` 構造体に、輸送を必要としているユニットのリスト（`transport_candidates`）を追加する。
- [x] 1.2 `strategy.rs` の `analyze_strategy` において、目標拠点から遠いユニットや、海に遮断されているユニットを検出し、その価値（戦闘力・占領能力）を評価するロジックを実装する。
- [x] 1.3 既存の輸送ユニット（輸送船、空母など）の数をカウントし、`existing_transport_count` に格納する。

## 2. 状態価値ベースのスコアリング実装

- [x] 2.1 `production.rs` の `calculate_unit_score_at` 関数において、輸送ユニットの一律ボーナス（+4000点）を廃止する。
- [x] 2.2 輸送ユニットごとに `loadable_unit_types` を参照し、`transport_candidates` の中から「実際に運べるユニット」のリストを抽出する。
- [x] 2.3 自力ETAと輸送時ETAの差分（ETA Gain）を算出し、運べるユニットの価値の総和に係数として乗算するロジックを実装する。
- [x] 2.4 高コストな輸送船（16500G）が、重戦車などの高価値ユニットを運ぶ際に適切に選ばれるよう、スコアの正規化を調整する。
- [x] 2.5 `existing_transport_count` に基づく需要減衰（1/(1+N)）を実装し、過剰生産を抑制する。

## 3. 検証と調整

- [x] 3.1 `map_2` において、自軍が戦車を保有している状況で、輸送ヘリではなく「輸送船」が優先的に生産されることを確認する。
- [x] 3.2 逆のケース（歩兵しかいない場合）では、引き続き安価な「輸送ヘリ」が選ばれることを確認し、経済的合理性が保たれていることを検証する。
- [x] 3.3 複数の島がある複雑な状況でも、輸送需要が適切に飽和し、過剰生産が行われないことを確認する。
